# Frontend

React + TypeScript + Vite client for the surf stats UI.

The frontend expects the backend API at `/api/maps`.

Useful commands:

```bash
npm run dev
npm run build
npm run lint
```

The root project README documents the full-stack setup and ingestion flow.

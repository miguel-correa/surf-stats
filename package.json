{
  "name": "surf-stats",
  "version": "1.0.0",
  "description": "A simple website to track map stats from KSF servers.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -c \"bgBlue.bold,bgGreen.bold\" -n \"BACKEND,FRONTEND\" \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "cd backend && go run ./cmd/server",
    "dev:frontend": "cd frontend && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
